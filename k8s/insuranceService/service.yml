apiVersion: v1
kind: Service
metadata:
  name: insurance-service
  namespace: production
spec:
  selector:
    app: insuranceService

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: ClusterIP