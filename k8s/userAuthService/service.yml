apiVersion: v1
kind: Service
metadata:
  name: user-auth-service
  namespace: production
spec:
  selector:
    app: userAuthService

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: ClusterIP